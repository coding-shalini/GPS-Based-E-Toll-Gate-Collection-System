name: gps_toll_gate_system
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.5 <4.0.0'

dependencies:
  firebase_core: ^2.24.0
  firebase_auth: ^4.12.1
  firebase_database: ^10.3.3
  location: ^5.0.3
  uuid: ^4.2.1
  intl: ^0.19.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
